{
  "name": "gestpalettets",
  "version": "1.0.0",
  "description": "",
  "main": "dist/electron/main.js",
  "scripts": {
    "build": "tsc && tsc -p tsconfig.renderer.json",
    "test": "vitest",
    "start": "tsc && tsc -p tsconfig.renderer.json && electron .",
    "dev": "concurrently --kill-others \"tsc -w\" \"tsc -p tsconfig.renderer.json -w\" \"nodemon --watch dist/electron --watch dist/db --ext js --delay 1 --exec electron .\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "sqlite3": "^6.0.1"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "electron": "^42.4.0",
    "jsdom": "^29.1.1",
    "nodemon": "^3.1.14",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
